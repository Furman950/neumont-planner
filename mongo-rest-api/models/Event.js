module.exports = class Event{
    constructor(id, userId, title, description, startTime, endTime){
        this.id = id,
        this.userId = userId,
        this.title = title,
        this.description = description,
        this.startTime = startTime,
        this.endTime = endtime
    }
}
